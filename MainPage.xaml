<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MusixMatchApiService.MainPage">

    <ScrollView>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="Center" Padding="10">
            <Image Source="music1.jpg" HeightRequest="180" WidthRequest="200"></Image>
            <Entry Text="{Binding search.Artist}" Placeholder="Artist" HeightRequest="35"/>
            <Entry Text="{Binding search.Title}" Placeholder="Title of the song" HeightRequest="35"/>

            <Button Text="Search" Command="{Binding GetLyricsCommand}" BackgroundColor="LightPink" TextColor="White" />
        </StackLayout>
    </ScrollView>
</ContentPage>
